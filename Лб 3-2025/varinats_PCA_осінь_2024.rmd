---
title: "_Лабораторна робота № 2_. Варіанти завдань"
author: "&copy; [Сидоренко В. М.](https://www.linkedin.com/in/valeriy-sydorenko-6782279a/), `r format(Sys.time(), '%Y')`"
date: "`r Sys.Date()`"
output:
#   pdf_document:
#     highlight: tango
#     toc: yes
  html_notebook:
    # citation_package: natbib
    toc: yes # Оглавление
    toc_float: true
    highlight: tango # Цвет подсветки кода
fontsize: 12pt # размер шрифта
header-includes:
 \usepackage[T2A]{fontenc}
 \usepackage[utf8]{inputenc}
 \usepackage[russian]{babel}
editor_options: 
  chunk_output_type: console
bibliography: references_lab.bib
---




# Варіанти по розвідувальному аналізу аналізу

```{r include=FALSE}
library(tidymodels)
```

## Датасет `PimaIndiansDiabetes`


```{r}

# Дані беремо з датасета PimaIndiansDiabetes пакета mlbench
library(mlbench)
data(PimaIndiansDiabetes)

diabetes_orig <- as_tibble(PimaIndiansDiabetes)
diabetes_orig

```



### Опис метаданих 

Набір даних містить інформацію про статус діабету жінок племені Піма.
 
Інформація про дані:
   
`pregnant` -- кількість вагітностей вагітностей;

`glucose` -- концентрація глюкози в крові;
 
`pressure` - артеріальний тиск;

`triceps` – товщина шкірної складки трицепса;

`insulin` - 2-годинний сироватковий інсулін;

`mass` – індекс маси тіла;

`pedigree` - функція родоводу;

`age` - вік;

`diabetes` - змінна класу (тест на діабет)


## Датасет `hw`

### Опис метаданих

`Зріст, см` -- зріст дорослої д.дини в сантиметрах  

`Вага, кг` -- маса тіла дорослої дюдинив кілограмах  

`Стать` -- стать людини


## Датасет `"heptathlon"`

```{r}
data("heptathlon", package = "HSAUR")
df <- as_tibble(heptathlon)
df
```

### Опис метаданих

Першою комбінованою олімпійською подією для жінок було п’ятиборство, яке вперше відбулося в Німеччині в 1928 році. Спочатку воно включало змагання з штовхання ядра, стрибків у довжину, бігу на 100 метрів, стрибків у висоту та метання списа, які проводилися протягом двох днів. П’ятиборство вперше було представлено на Олімпійських іграх у 1964 році, коли воно складалося з бігу на 80 метрів з бар’єрами, стрільби, стрибків у висоту, довжину та 200 метрів. У 1977 році 200 метрів було замінено на 800 метрів, а з 1981 року IAAF запровадила семиборство замість п’ятиборства, у перший день якого входили біг на 100 метрів з бар’єрами, стрільба, стрибки у висоту, 200 метрів, а другий день – стрибки в довжину. , метання списа та 800м. Система підрахунку балів використовується для присвоєння балів результатам кожного змагання, і переможцем стає жінка, яка набере найбільшу кількість балів за два дні. Подія вперше відбулася на Олімпійських іграх у 1984 році.

На Олімпіаді 1988 року в Сеулі семиборство виграла одна із зірок жіночої легкої атлетики США Джекі Джойнер-Керсі. Тут подано результати для всіх 25 учасників.

`hurdles` -- результати бігу на 100 м з бар'єрами

`highjump` -- результати стрибки у висоту

`shot` -- результати постріл

`run200m` -- результати бігу на 200 м

`longjump` -- результати стрибки в довжину

`javelin` -- результати метання списа

`run800m` -- результати бігу на 800 м

`score` -- загальний бал


## Датасет `CO2`

Фрейм даних CO2 містить 84 рядки та 5 стовпців даних з експерименту щодо холодостійкості трави Echinochloa crus-galli.

### Опис метаданих

`Plant` -- упорядкований фактор з рівнями Qn1 < Qn2 < Qn3 < ... < Mc1, що дає унікальний ідентифікатор для кожної рослини

`Type` -- фактор з рівнями Квебек Міссісіпі, що вказує на походження рослини

`Treatment` -- фактор з рівнями неохолоджені охолоджені

`conc` -- числовий вектор концентрації вуглекислого газу в навколишньому середовищі (мл/л).

`uptake` -- числовий вектор швидкості поглинання вуглекислого газу (𝜇моль/𝑚2 с)


```{r}
df <- as_tibble(CO2)
df
```


## Датасет `lung`

Датасет `lung` є одним з класичних наборів даних в пакеті `survival`, який використовується для демонстрації методів аналізу виживання. Він містить інформацію про пацієнтів з раком легень.

```{r}
library(survival)
df <- as_tibble(lung)
df
```


### Опис полів

Основні поля датасету:

`status` : Цей бінарний показник вказує на те, чи спостерігався за пацієнтом до настання події (смерті) або цензурування

`time` : Час від початку спостереження до події (смерті) або цензурування

`age` : Вік пацієнта на момент діагностики

`sex` : Стать пацієнта (зазвичай кодується як 1 для чоловіків і 2 для жінок)

`ph.ecog` : Зведений показник функціонального статусу за шкалою ECOG (Eastern Cooperative Oncology Group)

`diagtime` : Час від постановки діагнозу до початку лікування

`year` : Рік постановки діагнозу

`month` : Місяць постановки діагнозу

`day` : День постановки діагнозу

`age.at.diagnosis` : Вік пацієнта на момент постановки діагнозу (дублює поле age)

`status2` : Альтернативне кодування статусу

`futime` : Час спостереження (дублює поле time)


## Датасет `lung`

Це дані одного з перших успішних випробувань ад’ювантної хіміотерапії раку товстої кишки. Левамізол — малотоксичний препарат, який раніше використовувався для лікування глистових інвазій у тварин; 5-ФУ є помірно токсичним (як кажуть) хіміотерапевтичним засобом. Є два записи на особу, одна для рецидиву та одна для смерті

```{r}
library(survival)
df <- as_tibble(colon)
df
```


### Опис полів

d:	id

study:	1 for all patients

rx:	Treatment - Obs(ervation), Lev(amisole), Lev(amisole)+5-FU

sex:	1=male

age:	in years

obstruct:	obstruction of colon by tumour

perfor:	perforation of colon

adhere:	adherence to nearby organs

nodes:	number of lymph nodes with detectable cancer

time:	days until event or censoring

status:	censoring status

differ:	differentiation of tumour (1=well, 2=moderate, 3=poor)

extent:	Extent of local spread (1=submucosa, 2=muscle, 3=serosa, 4=contiguous structures)

surg:	time from surgery to registration (0=short, 1=long)

node4:	more than 4 positive lymph nodes

etype:	event type: 1=recurrence,2=death


## Датасет `ovarian`

Дані про виживання пацієнтів з раком яєчників.

```{r}
library(survival)
df <- as_tibble(colon)
df
```

### Опис полів

Аналогічно датасету `colon`


# Варіанти завдань 

1. Датасет `PimaIndiansDiabetes`. Виконати розвідувальний аналіз даних. Побудувати модель зниження розмірності на основі методу главних компонент
  

1. Датасет `hw`. Виконати розвідувальний аналіз даних . Побудувати модель зниження розмірності на основі методу главних компонент 

1. Датасет `heptathlon`. Виконати розвідувальний аналіз даних . Побудувати модель зниження розмірності на основі методу главних компонент     

1. Датасет `CO2`. Виконати розвідувальний аналіз даних . Побудувати модель зниження розмірності на основі методу главних компонент (для числових компонент)   

1. Датасет `lung`. Виконати розвідувальний аналіз даних . Побудувати модель зниження розмірності на основі методу главних компонент (для числових компонент)   

1. Датасет `colon`. Виконати розвідувальний аналіз даних . Побудувати модель зниження розмірності на основі методу главних компонент (для числових компонент)   

1. Датасет `ovarian`. Виконати розвідувальний аналіз даних . Побудувати модель зниження розмірності на основі методу главних компонент (для числових компонент)   





